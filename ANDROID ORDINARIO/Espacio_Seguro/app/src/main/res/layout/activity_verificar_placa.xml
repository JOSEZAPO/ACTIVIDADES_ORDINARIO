<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#FFFFFF"
    tools:context=".VerificarPlacaActivity">

    <!-- Título de la pantalla -->
    <TextView
        android:id="@+id/txtTituloVerificacion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Registrar Entrada"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_marginBottom="32dp"/>

    <!-- Campo de texto para la matrícula -->
    <!-- Campo de texto para la matrícula -->
    <EditText
        android:id="@+id/etPlaca"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="XXX-XXX-X"
        android:inputType="textCapCharacters|textNoSuggestions"
        android:background="@drawable/edit_text_background"
        android:padding="16dp"
        android:gravity="center"
        android:textSize="20sp"
        android:layout_marginBottom="24dp"
        android:autofillHints="none"
        android:maxLength="9" /> <!-- LÍNEA AÑADIDA PARA LIMITAR LA LONGITUD -->


    <!-- Contenedor para los botones Verificar y Cancelar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Botón de Verificar -->
        <Button
            android:id="@+id/btnVerificar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Verificar"
            android:backgroundTint="#4B61E7"
            android:textColor="#FFFFFF"
            android:layout_marginEnd="8dp"
            android:padding="12dp"
            android:textSize="16sp"/>

        <!-- Botón de Cancelar -->
        <Button
            android:id="@+id/btnCancelar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Cancelar"
            android:backgroundTint="#FF0000"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:textSize="16sp"/>

    </LinearLayout>

</LinearLayout>
